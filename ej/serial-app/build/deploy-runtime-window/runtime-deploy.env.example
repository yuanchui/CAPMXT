# 复制为 runtime-deploy.env 后填写（勿提交含密码的 runtime-deploy.env）

REMOTE_HOST=175.24.71.193
REMOTE_USER=root

# SSH 端口
SSH_PORT=22

# HTTP 服务端口（Python serve_runtime.py 监听，须与 runtime-window.config.json 中 remoteHttpPort 一致）
REMOTE_HTTP_PORT=19876

# 服务器数据目录（JSON 文件存放位置）
REMOTE_DIR=/var/www/mxt-runtime
REMOTE_FILE=runtime-window.generated.json

# SSH_KEY=C:\Users\you\.ssh\id_rsa
